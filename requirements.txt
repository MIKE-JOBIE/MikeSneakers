# Core web framework
Flask==3.1.1
Werkzeug==3.1.3

# Database ORM
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36

# Real-time notifications (dashboard alerts)
Flask-SocketIO==5.4.1
python-socketio==5.11.4
eventlet==0.37.0          # production-grade async server for Socket.IO

# Security
Flask-WTF==1.2.2          # CSRF protection
Flask-Limiter==3.9.2      # login / route rate limiting

# PDF export (sales history)
reportlab==4.2.5

# Production database drivers — uncomment the one you deploy with.
# SQLite (built into Python) is fine for development and a single-shop
# scale, but switch to Postgres once you're running 20-30 concurrent
# users so writes don't serialize behind SQLite's single-writer lock.
# psycopg2-binary==2.9.10      # PostgreSQL
# PyMySQL==1.1.1                # MySQL / MariaDB

# Production WSGI server (replaces `python app.py` / Flask dev server)
gunicorn==23.0.0

python-dotenv==1.0.1
